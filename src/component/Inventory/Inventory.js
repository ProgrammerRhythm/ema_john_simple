import React from 'react';
import order from '../../images/giphy.gif'
const inventory = () => {
    return (
        <div>
            <img src={order} alt="" />
        </div>
    );
};

export default inventory;